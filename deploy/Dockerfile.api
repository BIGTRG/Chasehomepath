# CHASE HomePath API — production container
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
COPY api/package.json api/
COPY web/package.json web/
RUN npm ci --omit=dev
COPY api/ api/
WORKDIR /app/api
EXPOSE 4000
CMD ["node", "src/index.js"]

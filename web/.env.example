# Web app env (Vite). In dev, /api is proxied to the Node API (see vite.config.js),
# so no API URL is needed. Set this only if you serve the API from another origin.
# VITE_API_URL=https://api.chasehomepath.example
